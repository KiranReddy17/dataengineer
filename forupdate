F-strings (formatted string literals) are a way to format strings in Python, which was introduced in Python 3.6. 
They make embedding expressions inside string literals easier and cleaner using curly braces {}.
To reverse a string using a function, you can use Python's slicing technique or a loop-based approach. Here's a simple function using slicing:
